# 독서 트래커 모듈 백로그

> 독서 트래커 기능의 추가 개선 사항을 우선순위 순으로 정리합니다.
> 상위 로드맵은 [`ROADMAP.md`](./ROADMAP.md) 참고.

## ✅ 완료
- [x] 카카오 책 API로 도서 검색
- [x] 검색 → 미리보기(가격·링크·목차) → 위시리스트/읽기 시작 흐름
- [x] 위시리스트 / 읽는중 / 완독 3단 책장 + 스와이프·메뉴 삭제
- [x] 목차 자동 가져오기 폴백 체인 (알라딘 API → 알라딘 웹 → NLK → **교보문고 SSR**)
- [x] 알라딘 가격·구매 링크 + 교보문고 링크 표시 (`url_launcher`)
- [x] 챕터별 체크박스 진행도 + 수동 목차 편집
- [x] 완독 처리 + 완독일 기록
- [x] **ISBN 단위 Postgres 캐싱** (Supabase, 30일 TTL, 27배 응답 향상)
- [x] **기간별 독서 통계** (오늘/주/월/년/전체, 월별 막대그래프)
- [x] SharedPreferences 로컬 저장

## 🧪 현재 단계: 실사용 검증
> 1~3단계 기능을 개인 사용하며 불편함·버그·개선점 수집 중.
> 로그인/클라우드 동기화(P4)는 실사용 피드백 이후 진행.

### Dogfooding 체크리스트
- [ ] 검색·등록·목차·진행도·완독 흐름이 매끄러운가
- [ ] 위시리스트 → 읽기 시작 → 완독 전환이 직관적인가
- [ ] 통계가 동기부여에 도움이 되는가
- [ ] 목차 자동 가져오기 성공률 (실패 도서 기록)
- [ ] UI/UX 불편 지점 메모

## 🎯 다음 우선순위 (실사용 후)

### P4. 소셜 로그인 + 클라우드 동기화 (보류 — 실사용 후 착수)
> 로그인만으로는 의미가 없으므로 **백엔드 + 동기화**를 함께 구축.

#### 4-1. 백엔드 셋업 (Supabase)
- Supabase 프로젝트 생성
- `books` 테이블 (user_id, 책 데이터)
- Row Level Security(RLS) 정책: 본인 데이터만 접근
- 로컬 → 클라우드 마이그레이션 로직 (기존 로컬 데이터 보존)

#### 4-2. 로그인 단계별 도입
1. **Google 로그인** (Supabase 기본 지원, 가장 빠름)
2. **카카오 로그인** (`kakao_flutter_sdk` + Supabase Edge Function으로 세션 교환)
3. **네이버 로그인** (`flutter_naver_login` + 동일 패턴)

#### 4-3. 동기화 전략
- 로컬 우선(Local-first), 백그라운드 동기화
- 충돌 해결: 마지막 수정 시간 기준(Last-Write-Wins) 또는 필드별 머지
- 오프라인 큐 (네트워크 끊김 시 변경 적재 후 재연결 시 flush)
- **예상 작업: 1~2일 (백엔드 + 3개 로그인 + 동기화)**

## 🔮 그 외 아이디어 (미확정)

- 교보문고 목차 프록시 (Supabase Edge Function) — 알라딘에 없는 책 대응
- AI 목차 추정 폴백 (Claude API)
- 일일 독서 시간 타이머 (포모도로 통합)
- 책 메모/하이라이트
- 독서 챌린지 (월 N권, 100일 챌린지)
- 친구·소셜 공유 (상용화 단계)
- 책 추천 (읽은 책 기반 큐레이션)

## 📦 상용화 직전 정리할 것

- [ ] 업데이트 배너의 진단 메시지(`최신 버전입니다`, `확인 중`, `확인 실패` 등) 숨김 처리 → 새 버전 있을 때만 표시
- [ ] 폰 화면에 디버그 텍스트 노출되는 부분 일괄 정리
- [ ] `lib/features/update/widgets/update_banner.dart`의 `_DebugBanner` 제거

## 💡 의사결정 필요 사항

- **상용화 시 결제 모델**: 위시리스트 무제한? 통계 고급 기능 유료?
- **데이터 백업 정책**: 클라우드 동기화 무료 / 프리미엄 분리?
- **광고 정책**: 자기계발 앱 톤상 광고 지양 권장 → 구독제 중심
